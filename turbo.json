{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "^prisma:generate"],
      "outputs": ["dist/**", ".next/**"],
      "cache": false
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "outputs": []
    },
    "prisma:generate": {
      "cache": false
    },
    "//#dev:sst": {
      "cache": false
    },
    "dev": {
      "dependsOn": ["prisma:generate", "^//#dev:sst"],
      "cache": false
    }
  }
}
